# E85 Restart Tune

Active E85 restart revisions live in `Revisions/`.

## Current Revision Order

1. `E85BaseMap.bin` - restart baseline.
2. `E85v2_hpfp.bin` - HPFP revision.
3. `E85v3(KillMap).bin` - current KillMap revision.

## Notes

- Older pre-restart E85 files are kept in `../E85_retired/`.
- New logs or change exports for this restart can go in `Logs/`.
